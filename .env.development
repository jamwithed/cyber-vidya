# Loaded only by `npm run dev` (Vite development mode).
# Points the student portal at the local backend so it works during development.
# For production, set VITE_API_BASE as a Cloudflare build env var to your hosted
# backend URL — until then the portal stays hidden / shows "launching soon".
VITE_API_BASE=http://localhost:4000
