import type { Testimonial } from '../types'

/**
 * Real learner testimonials only. The section is hidden while this is empty.
 *
 * To enable it, add entries like:
 * { name: 'Full Name', role: 'SOC Analyst · Company', quote: '…', rating: 5, initials: 'FN' }
 */
export const testimonials: Testimonial[] = []
